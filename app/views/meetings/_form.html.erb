<div class="modal-dialog modal-dialog-centered" role="document">
  <div class="modal-content">
    <%= form_with(model: @meeting, local: true) do |f| %>
    <% if @meeting.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@meeting.errors.count, "error") %> prohibited this meeting from being saved:</h2>
      <ul>
        <% @meeting.errors.full_messages.each do |message| %>
        <li><%= message %></li>
        <% end %>
      </ul>
    </div>
    <% end %>
    <div class="modal-header">
      <h5 class="modal-title">募集条件を記入してください<p>※空欄だと保存できません※</p></h5>
      <button type="button" class="close" data-dismiss="modal" aria-label="Close">
        <span aria-hidden="true">&times;</span>
      </button>
    </div>
    <div class="modal-body">

      <div class="form-group field">
        <%= f.label :"場所" , class: "form-control-label"%>
        <%= f.select :area, Meeting.areas.keys, {prompt: '選択してください'}, class: 'form-control' %>
      </div>
      <div class="form-group field">
        <%= f.label :"募集日" , class: "form-control-label"%>
        <%= f.date_field :date, class: "form-control" %>
      </div>
      <div class="form-group field">
        <%= f.label :"集合時間" , class: "form-control-label"%>
        <%= f.time_field :time, class: "form-control" %>
      </div>
      <div class="form-group field">
        <%= f.label :"お店の名前（最大100字）" , class: "form-control-label"%>
        <%= f.text_field :bar, maxlength: "100", class: "form-control" %>
      </div>
      <div class="form-group field">
        <%= f.label :"お店のURL（最大300字）" , class: "form-control-label"%>
        <%= f.url_field :url, maxlength: "300", class: "form-control" %>
      </div>
      <div class="form-group field">
        <%= f.label :"人数や、滞在時間について（最大500字）" , class: "form-control-label"%>
        <%= f.text_area :explain, maxlength: "500", class: "form-control" %>
      </div>
      <div class="form-group field">
        <%= f.label :"プロフィール画像", class: "form-control-label" %>
        <%= f.file_field :image, autocomplete: "image", class: "form-control" %>
      </div>
    </div>

    <div class="modal-footer actions">
      <%= f.submit "募集する", class: "btn btn-outline-primary"%>
    </div>

    <% end %>
  </div>
</div>